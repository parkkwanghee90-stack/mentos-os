import fs from 'fs';

const path = './public/concept_cards/premium_lectures.json';
const data = JSON.parse(fs.readFileSync(path, 'utf8'));

// Fix "지수함수" (id: 지수함수)
const exp = data['대수'].find(c => c.id === '지수함수');
if (exp) {
  exp.content = `# 📌 지수함수 완벽 정리
  
## 1. 지수함수의 그래프
- **밑 $a > 1$ 일 때**: $x$값이 커질수록 $y$값도 커지는 **증가함수**입니다. $a$가 커질수록 그래프는 $y$축에 더 가깝게 달라붙으며 가파르게 상승합니다.
- **밑 $0 < a < 1$ 일 때**: $x$값이 커질수록 $y$값은 작아지는 **감소함수**입니다. $a$가 작을수록(0에 가까울수록) 그래프는 더 급격하게 감소합니다.
- **공통 성질**: 반드시 점 $(0, 1)$을 지나며, $x$축($y=0$)이 **점근선**이 됩니다.

## 2. 지수방정식과 부등식
- **밑을 같게 할 수 있는 경우**: $a^{f(x)} = a^{g(x)} \\\\implies f(x) = g(x)$
- **지수부등식 주의사항**: 밑 $a$가 1보다 작으면 지수를 비교할 때 **부등호 방향이 반대**로 바뀝니다! ($a < 1 \\\\implies a^x < a^y \\\\iff x > y$)`;
}

// Fix "로그함수" (id: 로그함수)
const log = data['대수'].find(c => c.id === '로그함수');
if (log) {
  log.content = `# 📌 로그함수 완벽 정리

## 1. 로그함수의 그래프
- **지수함수와의 관계**: 로그함수 $y = \\\\log_a x$는 지수함수 $y = a^x$의 **역함수**이며, 직선 $y=x$에 대하여 대칭입니다.
- **밑 $a > 1$ 일 때**: 증가함수이며, 점 $(1, 0)$을 지납니다.
- **밑 $0 < a < 1$ 일 때**: 감소함수입니다.
- **점근선**: $y$축($x=0$)이 점근선입니다. **진수 조건($x > 0$)**에 의해 그래프는 $y$축 오른쪽에만 그려집니다.

## 2. 로그방정식과 부등식
- **진수 조건 체크 필수**: 문제를 풀기 전, 반드시 진수 부분이 0보다 크다는 범위를 먼저 잡아야 합니다.
- **밑 변환**: 밑이 다르면 밑 변환 공식을 통해 통일한 후 비교합니다.`;
}

// Fix "삼각함수성질" (id: 삼각함수성질)
const trig = data['대수'].find(c => c.id === '삼각함수성질');
if (trig) {
  trig.content = `# 📌 삼각함수의 성질 완벽 정리

## 1. 일반각과 호도법
- **일반각**: 동경이 나타내는 각은 하나가 아닙니다. $360^\\\\circ \\\\times n + \\\\alpha$ 로 표현합니다.
- **호도법**: 반지름과 호의 길이가 같을 때의 각을 $1$ 라디안이라 하며, $180^\\\\circ = \\\\pi$ 라디안입니다.
- **부채꼴**: 호의 길이 $l = r\\\\theta$, 넓이 $S = \\\\frac{1}{2}r^2\\\\theta = \\\\frac{1}{2}rl$

## 2. 삼각함수의 정의와 부호
- **단위원**: 반지름이 $r$인 원 위의 점 $P(x, y)$에 대해:
  - $\\\\sin \\\\theta = y/r, \\\\cos \\\\theta = x/r, \\\\tan \\\\theta = y/x$
- **사분면별 부호 (올사탄코)**:
  - 1사분면: **All** (+)
  - 2사분면: **Sin** (+)
  - 3사분면: **Tan** (+)
  - 4사분면: **Cos** (+)

## 3. 각변환 공식 ($n\\\\frac{\\\\pi}{2} \\\\pm \\\\theta$)
1. **함수 결정**: $n$이 홀수면 $\\\\sin \\\\leftrightarrow \\\\cos, \\\\tan \\\\leftrightarrow \\\\cot$ 로 바뀌고, $n$이 짝수면 그대로 유지됩니다.
2. **부호 결정**: 원래 함수가 해당 사분면에서 갖는 부호를 따릅니다.`;
}

fs.writeFileSync(path, JSON.stringify(data, null, 2));
console.log('Fixed Formula Card (premium_lectures.json) math with quadruple escaping!');
