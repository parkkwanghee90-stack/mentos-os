# 수학상 통합숙제 AVS 비전 힌트 — QA 리뷰 현황 (2026-06-11, 백로그 처리 완료)

## 범위
hw_09 고차방정식(20) · hw_10 일차부등식(13) · hw_11 이차부등식(30) · hw_12 경우의수(36) · hw_13 행렬(40) = **139문제**

크롭 파이프라인이 각 단원 꼬리에 만든 유령 슬롯 10개는 SSOT에서 제거됨. 상세: `homework_avs_asset_issues.json`

## 최종 상태: 잔여 백로그 0건

| 검증 | 결과 |
|---|---|
| 생성·업로드 (Supabase math_hints) | 139/139 |
| 구조(5필드·S≥3단계·백틱·모지바케·제어문자) | 위반 0 |
| KaTeX | 오류 0 |
| 적대적 QA + 수동 판정 | 전건 종결 (통과 130 + 판정 9) |

## 백로그 처리 이력 (2라운드)

**1라운드(--qa 피드백 재생성)**: 플래그 24건 중 11건 해소.
**2라운드(--qa 재생성 + 직접 판독 판정)**: 6건 추가 해소(11/004·028, 12/027, 13/028·032·039), 잔여 9건 전수 판정:

| 문제 | 판정 | 조치 |
|---|---|---|
| 09/016 | 부분 결함 (HC 유도 근거 "CD−AD" 비논리, 결론은 정확) | 등변사다리꼴 **중간선 정리**(MN=(AD+BC)/2=2r)로 해당 단계 교체 |
| 10/011 | 자기검토 문장 누출 ("0°가 되어 모순…가정하고") | **동측내각**(180°−120°=60°) 논증으로 해당 단계 교체 |
| 11/011 | 오탐 — 결론(ㄴ참·ㄷ거짓→②)이 원본·정답과 일치, QA가 원본에 없는 b<0 케이스 요구 | 없음 |
| 12/002 | 오탐 — 원본 해설이 정의한 인접관계 그대로(12+6=18), QA가 그림을 다르게 오독 | 없음 |
| 12/029 | 정상 — 원본(15×6=90, 20×2=40, 130)과 1:1 일치, QA 응답 파싱 실패였을 뿐 | 없음 |
| 12/030 | 실질 결함(1라운드에서 S가 78로 종결) → 수동 정정 후 재QA의 재플래그는 그림 오독 오탐 | 원본(126−63=63) 기준 수동 재작성 (완료) |
| 12/034 | 실질 결함 — 합(90)만 우연히 일치, 케이스 분배(6+60+24)가 원본(6+36+48)과 다름 | 원본 기준 **수동 재작성** |
| 12/035 | 오탐 — 묶음 풀이(80+80−16=144)가 원본과 동일 | 없음 |
| 13/021 | 오탐 — QA가 저장 정답 "3"(보기 ③, 값 6)을 값으로 오해 | 없음 |

## 교훈 (QA 그레이더 특성)
- 적대적 QA(gemini-2.5-flash)는 **도형·점배열 그림을 자주 오독**하며 실행마다 다르게 읽음(자기모순) → match/answerOk 플래그도 그림 문제는 직접 판독으로 재판정 필요.
- 정답 SSOT(avs_answers)는 판정 9건 전부에서 정확했음 — 신뢰 가능.
- 비전 생성 힌트의 전형적 결함: ①자기검토 문장 누출(새 표현 변형) ②케이스 분배를 합이 맞게 재구성 ③근거 비약. 합이 맞아도 분배가 원본과 다르면 의심.

부분 재처리: `node scripts/generate_homework_avs_vision.cjs <단원> --pids <목록> --force --qa` → `node scripts/verify_homework_avs.cjs ... --pids <목록>`
