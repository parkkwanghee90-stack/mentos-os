# WORKTREE - MATH SANG AVS FINAL DOCUMENTATION

- **작성 일시**: 2026-06-01 21:42 (KST)
- **대상 범위**: 수학(상) 2단계 ~ 4단계 AVS 칠판 해설 완전 복구 및 배포 검증 완료

---

## 📐 최종본 수학(상) AVS 복구 단원명 리스트

본 작업으로 mathmentos.com 실서버 및 로컬 전체에서 크래시 없이 완벽하게 복구된 수학(상)의 2단계, 3단계, 4단계 단원 리스트입니다:

1. **다항식 (2단계, 3단계, 4단계)**
   - Supabase CDN 폴더: `polynomial_step2`, `polynomial_step3`, `polynomial_step4`
2. **항등식과 나머지정리 (2단계, 3단계, 4단계)**
   - Supabase CDN 폴더: `remainder_step2`, `remainder_step3`, `remainder_step4`
3. **인수분해 (2단계, 3단계, 4단계)**
   - Supabase CDN 폴더: `factorization_step2`, `factorization_step3`, `factorization_step4`
4. **복소수 (2단계, 3단계, 4단계)**
   - Supabase CDN 폴더: `04_complex_numstep2`, `04_complex_numstep3`, `04_complex_numstep4`
5. **이차방정식 (2단계, 3단계, 4단계)**
   - Supabase CDN 폴더: `05_quad_eqstep2`, `05_quad_eqstep3`, `05_quad_eqstep4`
6. **이차방정식과 이차함수 (2단계, 3단계, 4단계)**
   - Supabase CDN 폴더: `06_quad_eq_funcstep2`, `06_quad_eq_funcstep3`, `06_quad_eq_funcstep4`

---

## 🛠️ 작업 수행 내용 요약

1. **구조적 경로 파싱 알고리즘 도입**:
   - `MathClassroom.jsx` 외 3개 핵심 페이지에서 `currentUnit`이 `"001.webp"`로 잘못 파싱되어 404/CORS fetch 무한로딩을 유발하던 정규식을 폐기하고, 확장자와 단독 단계를 정확하게 걸러내어 `(001)다항식` 등 원본 단원 폴더명을 추출해 주는 구조적 파싱 로직을 도입하였습니다.
2. **react-katex prop-types 런타임 크래시 해소**:
   - `mathmentos.com` 운영 번들러 환경에서 `react-katex`가 CJS `require("prop-types")`를 호출하여 전체 React 라우터를 정지시키던 치명적 결함을 발견하였습니다.
   - `react-katex` 임포트를 걷어내고 순수 `katex` 라이브러리로 직접 렌더링을 처리하는 커스텀 `BlockMath` 컴포넌트를 `AlgebraHintPlayer.jsx`에 안전하게 구현하여 100% 크래시 프리 수식 렌더러를 탑재했습니다.
3. **Vercel 클라우드 무중단 배포**:
   - 수정 완료된 소스 코드를 `git push origin main`으로 전송하여 Vercel 클라우드 빌드를 무사히 완료하고 실서버 패치를 완료했습니다.
